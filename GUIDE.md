# 帙雲 使用指南（繁體中文）

> 版本：v2.0｜平台：Google Apps Script + Microsoft Power Automate

---

## 目錄

1. [系統簡介](#1-系統簡介)
2. [建立平台前的準備工作](#2-建立平台前的準備工作)
3. [初始設定步驟（首次使用）](#3-初始設定步驟首次使用)
4. [部署 Web App（控制面板）](#4-部署-web-app控制面板)
5. [控制面板功能說明](#5-控制面板功能說明)
6. [班別及學生管理](#6-班別及學生管理)
7. [課業設置（佈置作業）](#7-課業設置佈置作業)
8. [學生如何提交功課](#8-學生如何提交功課)
9. [老師如何批改及發還課業](#9-老師如何批改及發還課業)
10. [繳交紀錄查閱](#10-繳交紀錄查閱)
11. [Microsoft Teams 自動追收功課設定](#11-microsoft-teams-自動追收功課設定)
12. [系統日誌及錯誤排查](#12-系統日誌及錯誤排查)
13. [常見問題](#13-常見問題)

---

## 1. 系統簡介

**帙雲**是一套以 Google Drive 為基礎的課業管理平台，主要功能包括：

| 功能 | 說明 |
|------|------|
| 📥 自動收集功課 | 學生上傳後，系統每分鐘自動整理至對應班別文件夾 |
| 📤 自動發還批改 | 老師上傳批改後，系統自動分發至學生專屬文件夾 |
| 📊 繳交紀錄追蹤 | 即時顯示每位學生的繳交狀態（已繳交 / 未繳交 / 遲交） |
| 🔔 自動追收提醒 | 未繳學生每日透過 Microsoft Teams 收到提醒 |
| 🗂️ 自動文件整理 | 每位學生有專屬文件夾，課業自動按類別分類 |
| 🌐 Web 控制面板 | 所有設定均可透過瀏覽器操作，無需打開試算表 |

---

## 2. 建立平台前的準備工作

### 所需帳號及工具

- **Google 帳號**（須有 Google Drive 及 Google Apps Script 存取權限）
- **GitHub 帳號**（僅用於下載程式碼，若直接使用提供的腳本則不需要）
- **Microsoft 帳號**（用於設置 Power Automate 流程）

### 複製 Apps Script 程式碼

1. 前往 [Google Apps Script](https://script.google.com)，點擊「新增專案」
2. 將以下各個 `.gs` 檔案的內容逐一貼入 Apps Script 編輯器：
   - `Shared.gs`（必須第一個建立，其他檔案均依賴此檔）
   - `ErrorLogger.gs`
   - `Setup.gs`
   - `CollectHomework.gs`
   - `AutoReturn.gs`
   - `AutoShare.gs`
   - `SubmissionRecord.gs`
   - `WebInterface.gs`
3. 將以下 HTML 檔案也貼入（點擊左上角「+」→「HTML」）：
   - `Index.html`
   - `record.html`
   - `homework.html`
   - `setup.html`

> **提示**：在 Apps Script 中，每個 `.gs` 檔案名稱不影響執行，但建議與原名稱相同以方便管理。

---

## 3. 初始設定步驟（首次使用）

### 方法一：透過 Web 控制面板設定（推薦）

完成下方「[部署 Web App](#4-部署-web-app控制面板)」後，在控制面板按「班別及學生管理」→「系統設定」分頁操作。

### 方法二：直接在 Apps Script 執行（進階用戶）

#### 步驟 1：建立根文件夾

1. 前往 [Google Drive](https://drive.google.com)
2. 建立一個新文件夾，例如命名為「帙雲」
3. 開啟該文件夾，從網址列複製文件夾 ID
   - 網址範例：`https://drive.google.com/drive/folders/`**`1AbCdEfGhIjKlMnOpQrStUv`**
   - 粗體部分即為文件夾 ID

#### 步驟 2：設定根文件夾 ID

開啟 `Shared.gs`，找到以下這行並替換：

```javascript
const ROOT_FOLDER_ID = 'YOUR_ROOT_FOLDER_ID_HERE';
// 改為：
const ROOT_FOLDER_ID = '1AbCdEfGhIjKlMnOpQrStUv'; // 換成你的 ID
```

同時修改電郵域名（如有需要）：

```javascript
const SCHOOL_EMAIL_DOMAIN = 'ccckyc.edu.hk'; // Google 帳號域名（共用文件夾用）
```

#### 步驟 3：執行 setup()

1. 在 Apps Script 編輯器頂端，從函數下拉選單選擇 **`setup`**
2. 點擊「▶ 執行」按鈕
3. 首次執行會彈出授權視窗，點擊「審閱權限」→ 選擇你的 Google 帳號 → 點擊「允許」
4. 執行完成後，點擊左下角「執行紀錄」查看輸出
5. 若看到「✅ 設置完成！」即代表成功

**執行紀錄範例（成功時）：**
```
✅ 根文件夾：帙雲 (1AbCdEfGhIjKlMnOpQrStUv)
📊 已建立試算表：自動共用、收集位址 (...)
📊 已建立試算表：繳交紀錄及課業佈置 (...)
📊 已建立試算表：OverdueAssignments (...)
✅ 所有 ID 已儲存至 Script Properties。
```

#### 步驟 4：建立自動觸發器

1. 在函數下拉選單選擇 **`createCollectTrigger`**，點擊「▶ 執行」
2. 選擇 **`createSubmissionTrigger`**，點擊「▶ 執行」
3. 選擇 **`createReturnTrigger`**，點擊「▶ 執行」

> **提示**：若使用 Web 控制面板，可在「系統設定」→「觸發器管理」一鍵建立所有觸發器。

**觸發器說明：**

| 觸發器 | 函數 | 頻率 | 功能 |
|--------|------|------|------|
| 收集功課 | `sortStudentAssignments` | 每 1 分鐘 | 將上傳區的檔案整理至待批改資料夾 |
| 繳交狀態更新 | `createFoldersAndUpdateSheet` | 每 5 分鐘 | 建立文件夾並更新繳交狀態 |
| 自動發還 | `distributeHomework` | 每 15 分鐘 | 將批改後課業分發至學生文件夾 |

---

## 4. 部署 Web App（控制面板）

1. 在 Apps Script 編輯器，點擊右上角「**部署**」→「**新增部署**」
2. 點擊齒輪圖示，選擇「**Web 應用程式**」
3. 填寫以下設定：
   - **說明**：帙雲 v1.0（或任意描述）
   - **以下列身份執行**：**我**（即你的 Google 帳號）
   - **誰可以存取**：依需要選擇（建議選「我的學校或機構中的任何人」）
4. 點擊「**部署**」，複製產生的 **Web App 網址**
5. 將此網址分發給老師使用（即控制面板入口）

> ⚠️ **重要**：每次修改程式碼後，如需更新 Web App，須重新部署（「部署」→「管理部署」→「編輯」→「新版本」→「部署」）。

---

## 5. 控制面板功能說明

開啟 Web App 網址後，即可看到控制面板，分為三大區域：

### 課業及回饋

| 按鈕 | 說明 |
|------|------|
| 📤 學生上傳區 | 學生將作業上傳至此 Google Drive 文件夾<br>**⚠️ 檔案命名格式**：`班別_姓名_關鍵詞`，例如：`1C_陳大文_寶物.pdf` |
| 📋 待批改課業 | 已整理好的課業，按班別→課業類別→課業名稱存放 |
| 💬 老師回饋區 | 老師上傳批改後課業至此，系統自動發還至學生文件夾 |
| ✅ 已發還課業 | 學生專屬文件夾，結構為：班別→學生姓名→課業類別→課業名稱 |

### 應用操作

| 按鈕 | 說明 |
|------|------|
| 📚 繳交紀錄 | 查看各班別學生的繳交狀態表（綠=已繳交、黃=遲交、紅=未繳交） |
| ⚙️ 課業設置 | 新增課業並設定截止日期，系統自動更新試算表及建立文件夾 |
| 📊 繳交紀錄試算表 | 直接開啟後台試算表查看原始資料 |
| 📊 共用位址試算表 | 查看學生專屬文件夾的分享連結 |

### 後臺設置

| 按鈕 | 說明 |
|------|------|
| ⚙️ 班別及學生管理 | 新增班別、設定學生名單及 Google 帳號，以及系統設定 |
| 📁 批量建立資料夾 | 依課業設置自動建立所有班別及學生的文件夾結構 |
| 🔗 自動共用專屬文件夾 | 將每位學生的「已發還課業」文件夾自動共用給其 Google 帳號 |

---

## 6. 班別及學生管理

在控制面板點擊「班別及學生管理」，進入管理頁面，共有四個分頁：

### 分頁 1：繳交紀錄管理

**新增班別：**
1. 在「班別名稱」欄輸入班別代號，例如 `1C`、`4A`
2. 點擊「新增」按鈕
3. 系統自動在「繳交紀錄及課業佈置」試算表建立對應分頁

**設定學生名單：**
1. 在班別清單點擊要編輯的班別（藍色表示已選取）
2. 在文字方塊輸入學生姓名，每行一個
3. 點擊「儲存名單」

> ⚠️ 儲存名單會**覆蓋**現有名單，請確認已包含所有學生。

### 分頁 2：學生帳號管理

此分頁用於設定學生的 Google 帳號，供「自動共用文件夾」及「Teams 通知」功能使用。

**設定方法：**
1. 選擇班別
2. 為每位學生填寫學號及姓名（學號即 Google 帳號前綴，格式：學號@學校域名）
3. 點擊「＋ 新增一行」可增加學生
4. 點擊「✕」可刪除該行
5. 點擊「儲存帳號」

**範例：**
- 學號：`23456`，姓名：`陳大文`
- 系統將使用電郵：`23456@ccckyc.edu.hk`（Google Drive 共用）

### 分頁 3：系統設定

此分頁可完成所有系統設定，**無需修改程式碼**：

**初始設置步驟：**
1. 在 Google Drive 建立根文件夾並複製 ID（網址中 `/folders/` 後的部分）
2. 在「Google Drive 根文件夾 ID」欄貼入 ID
3. 填寫 Google 帳號域名（如 `ccckyc.edu.hk`）
4. 填寫 Teams 電郵域名（如 `ms.ccckyc.edu.hk`）
5. 點擊「▶ 執行初始設置」

**系統資源狀態：**
- 顯示四個 Drive 文件夾及三個試算表是否已成功建立
- 點擊「開啟」可直接開啟對應資源

**觸發器管理：**
- 點擊「⚙ 建立所有觸發器」一鍵建立三個自動觸發器

### 分頁 4：系統日誌

顯示最近 100 筆系統記錄，包括：
- **錯誤（ERROR）**：紅色背景，需要處理的問題
- **警告（WARN）**：黃色背景，需要注意的情況
- **資訊（INFO）**：白色背景，正常操作記錄

可點擊「🔄 重新載入」更新，或「🗑 清除日誌」清空所有記錄。

---

## 7. 課業設置（佈置作業）

在控制面板點擊「課業設置」，進入佈置課業頁面：

1. **選擇班別**：從下拉選單選擇目標班別
2. **選擇課業類別**：閱讀 / 寫作（長文）/ 寫作（實用文）
3. **輸入課業名稱**：例如「藏在泥土的寶物」
4. **輸入關鍵詞**：例如「寶物」（用於識別學生上傳的檔案）
   > 📂 學生上傳檔案時，請以「**班別_姓名_關鍵詞**」命名，例如：`1C_陳大文_寶物.pdf`
5. **設定截止日期及時間**：依香港時間填寫
6. 點擊「**提交**」

系統會自動：
- 在「繳交紀錄及課業佈置」試算表記錄課業資訊
- 在「02_待批改課業」建立對應文件夾（下次觸發器執行時）

---

## 8. 學生如何提交功課

### 提交步驟

1. 完成功課後，將檔案重新命名，格式為：
   ```
   班別_姓名_關鍵詞.副檔名
   ```
   **例如：** `1C_陳大文_寶物.pdf`

2. 前往老師提供的「學生上傳區」Google Drive 連結
3. 將重新命名的檔案拖放或上傳至該文件夾
4. 系統每分鐘自動整理，無需其他操作

### 支援的檔案格式

| 格式 | 說明 |
|------|------|
| PDF | 推薦格式 |
| JPEG / JPG | 拍照提交 |
| PNG | 截圖提交 |
| GIF | 動態圖片 |
| BMP | 點陣圖 |
| WEBP | 現代圖片格式 |

> ⚠️ **注意事項：**
> - 檔案名稱必須包含班別（如 `1C`）、姓名（如 `陳大文`）及課業關鍵詞（如 `寶物`）
> - 若命名不正確，系統無法自動歸類，請確認後重新上傳
> - 同一課業可多次上傳，系統以最新一次為準

---

## 9. 老師如何批改及發還課業

### 批改流程

1. 前往「待批改課業」文件夾
2. 文件夾結構為：班別 → 課業類別 → 課業名稱
3. 下載需要批改的課業檔案
4. 完成批改後，將批改好的檔案**重新命名**，格式為：
   ```
   班別_姓名_關鍵詞（批改）.副檔名
   ```
   **例如：** `1C_陳大文_寶物（批改）.pdf`

### 發還流程

1. 前往「**老師回饋區**」文件夾（在控制面板點擊「老師回饋區」）
2. 將批改好的課業上傳至此文件夾
3. 系統每 15 分鐘自動執行，將檔案移動至以下路徑：
   ```
   04_已發還課業 / 【班別】/ 【學生姓名】/ 課業類別 / 課業關鍵詞文件夾
   ```

### 讓學生查看批改結果

1. 在控制面板點擊「**自動共用專屬文件夾**」
2. 系統自動將每位學生的「已發還課業」文件夾共用給其 Google 帳號
3. 學生登入 Google Drive 後即可查看批改結果
4. 共用連結亦記錄於「共用位址試算表」，可分發給學生

---

## 10. 繳交紀錄查閱

在控制面板點擊「**繳交紀錄**」，進入記錄查閱頁面：

### 頁面說明

- 頁面列出所有班別的課業繳交狀態
- 每個班別顯示一張表格，行為學生，列為課業

### 狀態顏色說明

| 顏色 | 狀態 | 說明 |
|------|------|------|
| 🟢 綠色 | 已繳交 | 在截止日期前提交 |
| 🟡 黃色 | 遲交 | 超過截止日期後才提交 |
| 🔴 紅色 | 未繳交 | 尚未提交 |

### 追收功課

- 系統每 5 分鐘自動更新狀態
- 「未繳交」且已過截止日期的學生，會自動加入逾期名單
- 透過 Power Automate 自動發送 Teams 提醒（見下一章）

---

## 11. Microsoft Teams 自動追收功課設定

此功能使用 **Microsoft Power Automate** 讀取「OverdueAssignments」試算表，自動向未繳交的學生發送 Teams 訊息。

### 準備工作

1. 確認「OverdueAssignments」試算表已建立（在系統設定中查看）
2. 確認「學生帳號管理」中已設定所有學生的學號及姓名
3. 確認在「系統設定」中填寫了正確的 **Teams 電郵域名**（如 `ms.ccckyc.edu.hk`）

### OverdueAssignments 試算表格式

系統每分鐘更新，格式如下：

| A：班別 | B：學生姓名 | C：學生電郵 | D：課業名稱 | E：截止日期 |
|---------|------------|------------|------------|------------|
| 1C | 陳大文 | 23456@ms.ccckyc.edu.hk | 藏在泥土的寶物 | 2025-04-29 23:59 |

### 建立 Power Automate 流程

#### 步驟 1：登入 Power Automate

1. 前往 [flow.microsoft.com](https://flow.microsoft.com)
2. 以學校 Microsoft 帳號登入

#### 步驟 2：建立新流程

1. 點擊左側「**建立**」→「**排程的雲端流程**」
2. 設定排程（建議每天執行 3 次，例如早上 8 點、下午 1 點、晚上 8 點）

#### 步驟 3：新增讀取試算表動作

1. 點擊「**+ 新增步驟**」
2. 搜尋「**Google Sheets**」，選擇「列出試算表中的列」
3. 設定：
   - **試算表**：選擇「OverdueAssignments」
   - **工作表**：選擇「Overdue Assignments」
4. 點擊「**+ 新增步驟**」
5. 搜尋「**Apply to each**」（套用至每個）

#### 步驟 4：新增發送 Teams 訊息動作

在「套用至每個」迴圈內：
1. 點擊「**新增動作**」
2. 搜尋「**Microsoft Teams**」→「發佈訊息為流程機器人到使用者」
3. 設定：
   - **收件者**：選擇「C：學生電郵」（動態內容）
   - **訊息**：
     ```
     親愛的同學，

     您有一份功課尚未繳交：
     📚 科目：中文
     📝 課業名稱：[D：課業名稱]
     ⏰ 截止日期：[E：截止日期]

     請盡快完成並上傳至學生上傳區，謝謝！
     ```
     （方括號內的部分使用動態內容替換）

#### 步驟 5：儲存並測試

1. 點擊「**儲存**」
2. 點擊「**測試**」→「**手動**」→「**執行流程**」
3. 確認學生收到 Teams 訊息

> **注意事項：**
> - Power Automate 中的 Google Sheets 連線器需要授權你的 Google 帳號
> - 學生的 Teams 帳號必須是其學校 Microsoft 帳號
> - 如需傳送至 Teams 頻道而非私訊，可選擇「在頻道中發佈訊息」

---

## 12. 系統日誌及錯誤排查

### 查看系統日誌

1. 前往控制面板 → 班別及學生管理 → **系統日誌**分頁
2. 點擊「🔄 重新載入」查看最新記錄
3. 日誌同時存儲於「繳交紀錄及課業佈置」試算表的「**系統日誌**」分頁

### 提供日誌給技術支援

1. 前往「繳交紀錄及課業佈置」試算表
2. 點擊「系統日誌」分頁
3. 選取所有資料（Ctrl+A），複製並發送給技術人員

### 常見錯誤代碼

| 錯誤訊息 | 原因 | 解決方法 |
|---------|------|---------|
| `尚未設定根文件夾 ID` | 未完成初始設置 | 前往「系統設定」執行初始設置 |
| `找不到「繳交紀錄及課業佈置」試算表` | 試算表被刪除或移動 | 重新執行初始設置 |
| `找不到班別文件夾：【1C】` | 文件夾不存在 | 點擊「批量建立資料夾」 |
| `共用失敗：陳大文` | 電郵地址錯誤或無存取權限 | 確認學生 Google 帳號設定 |
| `無法匹配班別，檔案留在原地` | 檔案命名不符規格 | 要求學生重新命名後再次上傳 |

### 手動重新執行功能

若自動觸發器未如期運作，可在控制面板手動觸發：
- **批量建立資料夾**：手動執行文件夾建立及狀態更新
- **自動共用專屬文件夾**：手動執行文件夾共用

---

## 13. 常見問題

**Q：學生上傳後多久才能看到狀態更新？**
A：系統每 1 分鐘自動收集，每 5 分鐘更新狀態，最多約 6 分鐘後狀態會更新。

**Q：觸發器多久執行一次？**
A：收集功課每 1 分鐘；狀態更新每 5 分鐘；自動發還每 15 分鐘。

**Q：為什麼有學生的文件夾 URL 是空白？**
A：需先執行「批量建立資料夾」後，再執行「自動共用專屬文件夾」，URL 才會自動填入。

**Q：修改了程式碼後，控制面板沒有更新？**
A：每次修改程式碼後，需在 Apps Script 重新部署 Web App（選擇「新版本」）。

**Q：部署後點擊按鈕沒有反應？**
A：確認 Web App 是以「我」的身份執行，且已授予所需權限。若仍有問題，嘗試重新部署。

**Q：如何讓學生直接看到「已發還課業」文件夾？**
A：執行「自動共用專屬文件夾」後，在「共用位址試算表」找到各學生的文件夾連結，分發給學生即可。

**Q：OverdueAssignments 試算表的腳本需要另外設置嗎？**
A：`OverdueAssignments.gs` 中的 `OVERDUE_ROOT_FOLDER_ID` 需要填寫根文件夾 ID，且需要在該腳本專案中單獨執行 `createTrigger()` 建立觸發器。這是因為它設計為可以獨立運作（例如綁定在 OverdueAssignments 試算表上）。

**Q：如何新增更多課業類別（如聆聽）？**
A：目前系統預設支援「閱讀」、「寫作（長文）」、「寫作（實用文）」三個類別。如需新增，請在 `SubmissionRecord.gs` 的 `createFoldersAndUpdateSheet()` 函數及 `homework.html` 的課業類別下拉選單中添加新類別。

---

*如有其他問題，請查看系統日誌或聯絡技術支援。*
